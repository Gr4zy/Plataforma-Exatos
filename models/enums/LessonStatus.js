// <<enum>> LessonStatus - exatamente como no diagrama de classes
const LessonStatus = Object.freeze({
  DRAFT: 'DRAFT',
  ARCHIVED: 'ARCHIVED',
  PUBLISHED: 'PUBLISHED',
});

module.exports = LessonStatus;
